$active_border = rgb({{ semantic.accent | strip_hash }})
$inactive_border = rgba({{ semantic.border | strip_hash }}aa)
$shadow_color = rgba({{ semantic.bg | strip_hash }}cc)
$shadow_inactive_color = rgba({{ semantic.bg | strip_hash }}66)
